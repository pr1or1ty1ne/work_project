function sendRec() {
    var recData = document.getElementById("sendRec").value;
    if recData = true {
        
    }
    else {
        alert("Please enter an email to send a receipt to.");
    }
}
